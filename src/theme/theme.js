import { createTheme } from '@mui/material/styles'; // Ensure you import from '@mui/material/styles'


const theme = createTheme({
  palette: {
    background: {
      paper: '#ffffff',
    },
    primary: {
      main: '#1976d2',
    },
  },
  spacing: 8,
});

export default theme;

